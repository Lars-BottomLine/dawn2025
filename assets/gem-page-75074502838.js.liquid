{% comment %}
	GEMPAGES BUILDER (https://apps.shopify.com/gempages)

	You SHOULD NOT modify source code in this file because
	It is automatically generated from GEMPAGES BUILDER
	Try to edit page with the live editor.
{% endcomment %}

    
      
    
    
    
    
jQuery(function() {
  var mode = jQuery('.gryffeditor').hasClass('editing') ? 'dev' : 'production';
  var $module = jQuery('#m-1612122066054').children('.module');
  if (mode == 'dev') {
      jQuery('#m-1612122066054').attr('data-name', '').css('background-image', 'none').removeAttr('data-image');
      
      var flag = true;
      var $bkLiquid = parent.jQuery('body').find('#gfFrame').contents().find('#module-1612122066054');
      if ($bkLiquid && $bkLiquid.length > 0) {
          var $settings = $bkLiquid.find('.settings');
          try {
              var name = '';
              var imageUrl = '';
              settings = JSON.parse($settings.html());
              for (var i = 0; i < settings.length; i++) {
                  if (settings[i].name == 'name') {
                      name = settings[i].default_value
                  }
                  if (settings[i].name == 'image') {
                      imageUrl = settings[i].default_value
                  }
              }
              if (imageUrl != '') {
                  flag = false;
                  jQuery('#m-1612122066054').css('background-image', 'url(' + imageUrl + ')').css('min-height', '100px').attr('data-image', 'true');
              }
              if (name != '' && name != 'Custom Code') {
                  flag = false;
                  jQuery('#m-1612122066054').attr('data-name', name);
              }
          } catch(error) {
              console.log(error);
          }
      }
      if (flag) {
          jQuery('#m-1612122066054').attr('data-name', 'Right click on the module, then choose Edit Html / Liquid option to start writing your custom code.');
      }
  }
});
    
    
(function( jQuery ){
  var $module = jQuery('#m-1643406709272').children('.module');
  var videoUrl = jQuery.trim($module.attr('data-url'));
  var autoPause = $module.attr('data-autopause');
  var autoPlay = $module.attr('data-autoplay');
  var videoloop = $module.attr('data-videoloop');
  var videoMute = $module.attr('data-videomute');
  var byline = $module.attr('data-byline');
  var showtitle = $module.attr('data-showtitle');
  var showportrait = $module.attr('data-showportrait');

  setTimeout(function() {
    var videoWrapper = $module.children('.vimeo_video');
    videoWrapper.empty().append('<div id="vimeo-wrapper-1643406709272"></div>');

    var options = {
      url: videoUrl,
      loop: videoloop,
      autoplay: autoPlay,
      autopause: autoPause,
      byline: byline,
      title: showtitle,
      muted: videoMute,
      portrait: showportrait
    };
    var player = new Vimeo.Player("vimeo-wrapper-1643406709272", options);

    player.on('loaded', function() {	
      var $playerFrame = $module.find('iframe');	
      var width = parseInt($playerFrame.attr('width'));	
      var height = parseInt($playerFrame.attr('height'));	
      if (width != NaN && height != NaN) {	
        $module.find('.videoFullScreen').css('padding-bottom', (height / width * 100) + '%');	
      }	
    });	
    
    $module.on('pausevideo', function() {	
      player.pause();	
    });
  }, 100);
})( window.GemQuery || jQuery );
    
    
(function( jQuery ){
  var $module = jQuery('#m-1646576935849').children('.module');
  var videoUrl = jQuery.trim($module.attr('data-url'));
  var autoPause = $module.attr('data-autopause');
  var autoPlay = $module.attr('data-autoplay');
  var videoloop = $module.attr('data-videoloop');
  var videoMute = $module.attr('data-videomute');
  var byline = $module.attr('data-byline');
  var showtitle = $module.attr('data-showtitle');
  var showportrait = $module.attr('data-showportrait');

  setTimeout(function() {
    var videoWrapper = $module.children('.vimeo_video');
    videoWrapper.empty().append('<div id="vimeo-wrapper-1646576935849"></div>');

    var options = {
      url: videoUrl,
      loop: videoloop,
      autoplay: autoPlay,
      autopause: autoPause,
      byline: byline,
      title: showtitle,
      muted: videoMute,
      portrait: showportrait
    };
    var player = new Vimeo.Player("vimeo-wrapper-1646576935849", options);

    player.on('loaded', function() {	
      var $playerFrame = $module.find('iframe');	
      var width = parseInt($playerFrame.attr('width'));	
      var height = parseInt($playerFrame.attr('height'));	
      if (width != NaN && height != NaN) {	
        $module.find('.videoFullScreen').css('padding-bottom', (height / width * 100) + '%');	
      }	
    });	
    
    $module.on('pausevideo', function() {	
      player.pause();	
    });
  }, 100);
})( window.GemQuery || jQuery );
    
    
(function( jQuery ){
  var $module = jQuery('#m-1643406748683').children('.module');
  var videoUrl = jQuery.trim($module.attr('data-url'));
  var autoPause = $module.attr('data-autopause');
  var autoPlay = $module.attr('data-autoplay');
  var videoloop = $module.attr('data-videoloop');
  var videoMute = $module.attr('data-videomute');
  var byline = $module.attr('data-byline');
  var showtitle = $module.attr('data-showtitle');
  var showportrait = $module.attr('data-showportrait');

  setTimeout(function() {
    var videoWrapper = $module.children('.vimeo_video');
    videoWrapper.empty().append('<div id="vimeo-wrapper-1643406748683"></div>');

    var options = {
      url: videoUrl,
      loop: videoloop,
      autoplay: autoPlay,
      autopause: autoPause,
      byline: byline,
      title: showtitle,
      muted: videoMute,
      portrait: showportrait
    };
    var player = new Vimeo.Player("vimeo-wrapper-1643406748683", options);

    player.on('loaded', function() {	
      var $playerFrame = $module.find('iframe');	
      var width = parseInt($playerFrame.attr('width'));	
      var height = parseInt($playerFrame.attr('height'));	
      if (width != NaN && height != NaN) {	
        $module.find('.videoFullScreen').css('padding-bottom', (height / width * 100) + '%');	
      }	
    });	
    
    $module.on('pausevideo', function() {	
      player.pause();	
    });
  }, 100);
})( window.GemQuery || jQuery );
    
    
(function( jQuery ){
  var $module = jQuery('#m-1643406792592').children('.module');
  var videoUrl = jQuery.trim($module.attr('data-url'));
  var autoPause = $module.attr('data-autopause');
  var autoPlay = $module.attr('data-autoplay');
  var videoloop = $module.attr('data-videoloop');
  var videoMute = $module.attr('data-videomute');
  var byline = $module.attr('data-byline');
  var showtitle = $module.attr('data-showtitle');
  var showportrait = $module.attr('data-showportrait');

  setTimeout(function() {
    var videoWrapper = $module.children('.vimeo_video');
    videoWrapper.empty().append('<div id="vimeo-wrapper-1643406792592"></div>');

    var options = {
      url: videoUrl,
      loop: videoloop,
      autoplay: autoPlay,
      autopause: autoPause,
      byline: byline,
      title: showtitle,
      muted: videoMute,
      portrait: showportrait
    };
    var player = new Vimeo.Player("vimeo-wrapper-1643406792592", options);

    player.on('loaded', function() {	
      var $playerFrame = $module.find('iframe');	
      var width = parseInt($playerFrame.attr('width'));	
      var height = parseInt($playerFrame.attr('height'));	
      if (width != NaN && height != NaN) {	
        $module.find('.videoFullScreen').css('padding-bottom', (height / width * 100) + '%');	
      }	
    });	
    
    $module.on('pausevideo', function() {	
      player.pause();	
    });
  }, 100);
})( window.GemQuery || jQuery );
    
    
(function( jQuery ){
  var $module = jQuery('#m-1643406822119').children('.module');
  var videoUrl = jQuery.trim($module.attr('data-url'));
  var autoPause = $module.attr('data-autopause');
  var autoPlay = $module.attr('data-autoplay');
  var videoloop = $module.attr('data-videoloop');
  var videoMute = $module.attr('data-videomute');
  var byline = $module.attr('data-byline');
  var showtitle = $module.attr('data-showtitle');
  var showportrait = $module.attr('data-showportrait');

  setTimeout(function() {
    var videoWrapper = $module.children('.vimeo_video');
    videoWrapper.empty().append('<div id="vimeo-wrapper-1643406822119"></div>');

    var options = {
      url: videoUrl,
      loop: videoloop,
      autoplay: autoPlay,
      autopause: autoPause,
      byline: byline,
      title: showtitle,
      muted: videoMute,
      portrait: showportrait
    };
    var player = new Vimeo.Player("vimeo-wrapper-1643406822119", options);

    player.on('loaded', function() {	
      var $playerFrame = $module.find('iframe');	
      var width = parseInt($playerFrame.attr('width'));	
      var height = parseInt($playerFrame.attr('height'));	
      if (width != NaN && height != NaN) {	
        $module.find('.videoFullScreen').css('padding-bottom', (height / width * 100) + '%');	
      }	
    });	
    
    $module.on('pausevideo', function() {	
      player.pause();	
    });
  }, 100);
})( window.GemQuery || jQuery );
    
    
(function( jQuery ){
  var $module = jQuery('#m-1643406854298').children('.module');
  var videoUrl = jQuery.trim($module.attr('data-url'));
  var autoPause = $module.attr('data-autopause');
  var autoPlay = $module.attr('data-autoplay');
  var videoloop = $module.attr('data-videoloop');
  var videoMute = $module.attr('data-videomute');
  var byline = $module.attr('data-byline');
  var showtitle = $module.attr('data-showtitle');
  var showportrait = $module.attr('data-showportrait');

  setTimeout(function() {
    var videoWrapper = $module.children('.vimeo_video');
    videoWrapper.empty().append('<div id="vimeo-wrapper-1643406854298"></div>');

    var options = {
      url: videoUrl,
      loop: videoloop,
      autoplay: autoPlay,
      autopause: autoPause,
      byline: byline,
      title: showtitle,
      muted: videoMute,
      portrait: showportrait
    };
    var player = new Vimeo.Player("vimeo-wrapper-1643406854298", options);

    player.on('loaded', function() {	
      var $playerFrame = $module.find('iframe');	
      var width = parseInt($playerFrame.attr('width'));	
      var height = parseInt($playerFrame.attr('height'));	
      if (width != NaN && height != NaN) {	
        $module.find('.videoFullScreen').css('padding-bottom', (height / width * 100) + '%');	
      }	
    });	
    
    $module.on('pausevideo', function() {	
      player.pause();	
    });
  }, 100);
})( window.GemQuery || jQuery );
    
    
(function( jQuery ){
  var $module = jQuery('#m-1643406889055').children('.module');
  var videoUrl = jQuery.trim($module.attr('data-url'));
  var autoPause = $module.attr('data-autopause');
  var autoPlay = $module.attr('data-autoplay');
  var videoloop = $module.attr('data-videoloop');
  var videoMute = $module.attr('data-videomute');
  var byline = $module.attr('data-byline');
  var showtitle = $module.attr('data-showtitle');
  var showportrait = $module.attr('data-showportrait');

  setTimeout(function() {
    var videoWrapper = $module.children('.vimeo_video');
    videoWrapper.empty().append('<div id="vimeo-wrapper-1643406889055"></div>');

    var options = {
      url: videoUrl,
      loop: videoloop,
      autoplay: autoPlay,
      autopause: autoPause,
      byline: byline,
      title: showtitle,
      muted: videoMute,
      portrait: showportrait
    };
    var player = new Vimeo.Player("vimeo-wrapper-1643406889055", options);

    player.on('loaded', function() {	
      var $playerFrame = $module.find('iframe');	
      var width = parseInt($playerFrame.attr('width'));	
      var height = parseInt($playerFrame.attr('height'));	
      if (width != NaN && height != NaN) {	
        $module.find('.videoFullScreen').css('padding-bottom', (height / width * 100) + '%');	
      }	
    });	
    
    $module.on('pausevideo', function() {	
      player.pause();	
    });
  }, 100);
})( window.GemQuery || jQuery );
    
    
(function( jQuery ){
  var $module = jQuery('#m-1643406924358').children('.module');
  var videoUrl = jQuery.trim($module.attr('data-url'));
  var autoPause = $module.attr('data-autopause');
  var autoPlay = $module.attr('data-autoplay');
  var videoloop = $module.attr('data-videoloop');
  var videoMute = $module.attr('data-videomute');
  var byline = $module.attr('data-byline');
  var showtitle = $module.attr('data-showtitle');
  var showportrait = $module.attr('data-showportrait');

  setTimeout(function() {
    var videoWrapper = $module.children('.vimeo_video');
    videoWrapper.empty().append('<div id="vimeo-wrapper-1643406924358"></div>');

    var options = {
      url: videoUrl,
      loop: videoloop,
      autoplay: autoPlay,
      autopause: autoPause,
      byline: byline,
      title: showtitle,
      muted: videoMute,
      portrait: showportrait
    };
    var player = new Vimeo.Player("vimeo-wrapper-1643406924358", options);

    player.on('loaded', function() {	
      var $playerFrame = $module.find('iframe');	
      var width = parseInt($playerFrame.attr('width'));	
      var height = parseInt($playerFrame.attr('height'));	
      if (width != NaN && height != NaN) {	
        $module.find('.videoFullScreen').css('padding-bottom', (height / width * 100) + '%');	
      }	
    });	
    
    $module.on('pausevideo', function() {	
      player.pause();	
    });
  }, 100);
})( window.GemQuery || jQuery );
    
    
(function( jQuery ){
  var $module = jQuery('#m-1643406955279').children('.module');
  var videoUrl = jQuery.trim($module.attr('data-url'));
  var autoPause = $module.attr('data-autopause');
  var autoPlay = $module.attr('data-autoplay');
  var videoloop = $module.attr('data-videoloop');
  var videoMute = $module.attr('data-videomute');
  var byline = $module.attr('data-byline');
  var showtitle = $module.attr('data-showtitle');
  var showportrait = $module.attr('data-showportrait');

  setTimeout(function() {
    var videoWrapper = $module.children('.vimeo_video');
    videoWrapper.empty().append('<div id="vimeo-wrapper-1643406955279"></div>');

    var options = {
      url: videoUrl,
      loop: videoloop,
      autoplay: autoPlay,
      autopause: autoPause,
      byline: byline,
      title: showtitle,
      muted: videoMute,
      portrait: showportrait
    };
    var player = new Vimeo.Player("vimeo-wrapper-1643406955279", options);

    player.on('loaded', function() {	
      var $playerFrame = $module.find('iframe');	
      var width = parseInt($playerFrame.attr('width'));	
      var height = parseInt($playerFrame.attr('height'));	
      if (width != NaN && height != NaN) {	
        $module.find('.videoFullScreen').css('padding-bottom', (height / width * 100) + '%');	
      }	
    });	
    
    $module.on('pausevideo', function() {	
      player.pause();	
    });
  }, 100);
})( window.GemQuery || jQuery );
    
    
(function( jQuery ){
  var $module = jQuery('#m-1643406984535').children('.module');
  var videoUrl = jQuery.trim($module.attr('data-url'));
  var autoPause = $module.attr('data-autopause');
  var autoPlay = $module.attr('data-autoplay');
  var videoloop = $module.attr('data-videoloop');
  var videoMute = $module.attr('data-videomute');
  var byline = $module.attr('data-byline');
  var showtitle = $module.attr('data-showtitle');
  var showportrait = $module.attr('data-showportrait');

  setTimeout(function() {
    var videoWrapper = $module.children('.vimeo_video');
    videoWrapper.empty().append('<div id="vimeo-wrapper-1643406984535"></div>');

    var options = {
      url: videoUrl,
      loop: videoloop,
      autoplay: autoPlay,
      autopause: autoPause,
      byline: byline,
      title: showtitle,
      muted: videoMute,
      portrait: showportrait
    };
    var player = new Vimeo.Player("vimeo-wrapper-1643406984535", options);

    player.on('loaded', function() {	
      var $playerFrame = $module.find('iframe');	
      var width = parseInt($playerFrame.attr('width'));	
      var height = parseInt($playerFrame.attr('height'));	
      if (width != NaN && height != NaN) {	
        $module.find('.videoFullScreen').css('padding-bottom', (height / width * 100) + '%');	
      }	
    });	
    
    $module.on('pausevideo', function() {	
      player.pause();	
    });
  }, 100);
})( window.GemQuery || jQuery );
    
    
(function( jQuery ){
  var $module = jQuery('#m-1643407027117').children('.module');
  var videoUrl = jQuery.trim($module.attr('data-url'));
  var autoPause = $module.attr('data-autopause');
  var autoPlay = $module.attr('data-autoplay');
  var videoloop = $module.attr('data-videoloop');
  var videoMute = $module.attr('data-videomute');
  var byline = $module.attr('data-byline');
  var showtitle = $module.attr('data-showtitle');
  var showportrait = $module.attr('data-showportrait');

  setTimeout(function() {
    var videoWrapper = $module.children('.vimeo_video');
    videoWrapper.empty().append('<div id="vimeo-wrapper-1643407027117"></div>');

    var options = {
      url: videoUrl,
      loop: videoloop,
      autoplay: autoPlay,
      autopause: autoPause,
      byline: byline,
      title: showtitle,
      muted: videoMute,
      portrait: showportrait
    };
    var player = new Vimeo.Player("vimeo-wrapper-1643407027117", options);

    player.on('loaded', function() {	
      var $playerFrame = $module.find('iframe');	
      var width = parseInt($playerFrame.attr('width'));	
      var height = parseInt($playerFrame.attr('height'));	
      if (width != NaN && height != NaN) {	
        $module.find('.videoFullScreen').css('padding-bottom', (height / width * 100) + '%');	
      }	
    });	
    
    $module.on('pausevideo', function() {	
      player.pause();	
    });
  }, 100);
})( window.GemQuery || jQuery );
    
    
(function( jQuery ){
  var $module = jQuery('#m-1646388810462').children('.module');
  var videoUrl = jQuery.trim($module.attr('data-url'));
  var autoPause = $module.attr('data-autopause');
  var autoPlay = $module.attr('data-autoplay');
  var videoloop = $module.attr('data-videoloop');
  var videoMute = $module.attr('data-videomute');
  var byline = $module.attr('data-byline');
  var showtitle = $module.attr('data-showtitle');
  var showportrait = $module.attr('data-showportrait');

  setTimeout(function() {
    var videoWrapper = $module.children('.vimeo_video');
    videoWrapper.empty().append('<div id="vimeo-wrapper-1646388810462"></div>');

    var options = {
      url: videoUrl,
      loop: videoloop,
      autoplay: autoPlay,
      autopause: autoPause,
      byline: byline,
      title: showtitle,
      muted: videoMute,
      portrait: showportrait
    };
    var player = new Vimeo.Player("vimeo-wrapper-1646388810462", options);

    player.on('loaded', function() {	
      var $playerFrame = $module.find('iframe');	
      var width = parseInt($playerFrame.attr('width'));	
      var height = parseInt($playerFrame.attr('height'));	
      if (width != NaN && height != NaN) {	
        $module.find('.videoFullScreen').css('padding-bottom', (height / width * 100) + '%');	
      }	
    });	
    
    $module.on('pausevideo', function() {	
      player.pause();	
    });
  }, 100);
})( window.GemQuery || jQuery );
    
    
(function( jQuery ){
  var $module = jQuery('#m-1643407060064').children('.module');
  var videoUrl = jQuery.trim($module.attr('data-url'));
  var autoPause = $module.attr('data-autopause');
  var autoPlay = $module.attr('data-autoplay');
  var videoloop = $module.attr('data-videoloop');
  var videoMute = $module.attr('data-videomute');
  var byline = $module.attr('data-byline');
  var showtitle = $module.attr('data-showtitle');
  var showportrait = $module.attr('data-showportrait');

  setTimeout(function() {
    var videoWrapper = $module.children('.vimeo_video');
    videoWrapper.empty().append('<div id="vimeo-wrapper-1643407060064"></div>');

    var options = {
      url: videoUrl,
      loop: videoloop,
      autoplay: autoPlay,
      autopause: autoPause,
      byline: byline,
      title: showtitle,
      muted: videoMute,
      portrait: showportrait
    };
    var player = new Vimeo.Player("vimeo-wrapper-1643407060064", options);

    player.on('loaded', function() {	
      var $playerFrame = $module.find('iframe');	
      var width = parseInt($playerFrame.attr('width'));	
      var height = parseInt($playerFrame.attr('height'));	
      if (width != NaN && height != NaN) {	
        $module.find('.videoFullScreen').css('padding-bottom', (height / width * 100) + '%');	
      }	
    });	
    
    $module.on('pausevideo', function() {	
      player.pause();	
    });
  }, 100);
})( window.GemQuery || jQuery );
    
    
(function( jQuery ){
  var $module = jQuery('#m-1670584793276').children('.module');
  var videoUrl = jQuery.trim($module.attr('data-url'));
  var autoPause = $module.attr('data-autopause');
  var autoPlay = $module.attr('data-autoplay');
  var videoloop = $module.attr('data-videoloop');
  var videoMute = $module.attr('data-videomute');
  var byline = $module.attr('data-byline');
  var showtitle = $module.attr('data-showtitle');
  var showportrait = $module.attr('data-showportrait');

  setTimeout(function() {
    var videoWrapper = $module.children('.vimeo_video');
    videoWrapper.empty().append('<div id="vimeo-wrapper-1670584793276"></div>');

    var options = {
      url: videoUrl,
      loop: videoloop,
      autoplay: autoPlay,
      autopause: autoPause,
      byline: byline,
      title: showtitle,
      muted: videoMute,
      portrait: showportrait
    };
    var player = new Vimeo.Player("vimeo-wrapper-1670584793276", options);

    player.on('loaded', function() {	
      var $playerFrame = $module.find('iframe');	
      var width = parseInt($playerFrame.attr('width'));	
      var height = parseInt($playerFrame.attr('height'));	
      if (width != NaN && height != NaN) {	
        $module.find('.videoFullScreen').css('padding-bottom', (height / width * 100) + '%');	
      }	
    });	
    
    $module.on('pausevideo', function() {	
      player.pause();	
    });
  }, 100);
})( window.GemQuery || jQuery );
    
    
(function( jQuery ){
  var $module = jQuery('#m-1643407087018').children('.module');
  var videoUrl = jQuery.trim($module.attr('data-url'));
  var autoPause = $module.attr('data-autopause');
  var autoPlay = $module.attr('data-autoplay');
  var videoloop = $module.attr('data-videoloop');
  var videoMute = $module.attr('data-videomute');
  var byline = $module.attr('data-byline');
  var showtitle = $module.attr('data-showtitle');
  var showportrait = $module.attr('data-showportrait');

  setTimeout(function() {
    var videoWrapper = $module.children('.vimeo_video');
    videoWrapper.empty().append('<div id="vimeo-wrapper-1643407087018"></div>');

    var options = {
      url: videoUrl,
      loop: videoloop,
      autoplay: autoPlay,
      autopause: autoPause,
      byline: byline,
      title: showtitle,
      muted: videoMute,
      portrait: showportrait
    };
    var player = new Vimeo.Player("vimeo-wrapper-1643407087018", options);

    player.on('loaded', function() {	
      var $playerFrame = $module.find('iframe');	
      var width = parseInt($playerFrame.attr('width'));	
      var height = parseInt($playerFrame.attr('height'));	
      if (width != NaN && height != NaN) {	
        $module.find('.videoFullScreen').css('padding-bottom', (height / width * 100) + '%');	
      }	
    });	
    
    $module.on('pausevideo', function() {	
      player.pause();	
    });
  }, 100);
})( window.GemQuery || jQuery );
    
    
(function( jQuery ){
  var $module = jQuery('#m-1687463065691').children('.module');
  var videoUrl = jQuery.trim($module.attr('data-url'));
  var autoPause = $module.attr('data-autopause');
  var autoPlay = $module.attr('data-autoplay');
  var videoloop = $module.attr('data-videoloop');
  var videoMute = $module.attr('data-videomute');
  var byline = $module.attr('data-byline');
  var showtitle = $module.attr('data-showtitle');
  var showportrait = $module.attr('data-showportrait');

  setTimeout(function() {
    var videoWrapper = $module.children('.vimeo_video');
    videoWrapper.empty().append('<div id="vimeo-wrapper-1687463065691"></div>');

    var options = {
      url: videoUrl,
      loop: videoloop,
      autoplay: autoPlay,
      autopause: autoPause,
      byline: byline,
      title: showtitle,
      muted: videoMute,
      portrait: showportrait
    };
    var player = new Vimeo.Player("vimeo-wrapper-1687463065691", options);

    player.on('loaded', function() {	
      var $playerFrame = $module.find('iframe');	
      var width = parseInt($playerFrame.attr('width'));	
      var height = parseInt($playerFrame.attr('height'));	
      if (width != NaN && height != NaN) {	
        $module.find('.videoFullScreen').css('padding-bottom', (height / width * 100) + '%');	
      }	
    });	
    
    $module.on('pausevideo', function() {	
      player.pause();	
    });
  }, 100);
})( window.GemQuery || jQuery );
    
    
(function( jQuery ){
  var $module = jQuery('#m-1643407133519').children('.module');
  var videoUrl = jQuery.trim($module.attr('data-url'));
  var autoPause = $module.attr('data-autopause');
  var autoPlay = $module.attr('data-autoplay');
  var videoloop = $module.attr('data-videoloop');
  var videoMute = $module.attr('data-videomute');
  var byline = $module.attr('data-byline');
  var showtitle = $module.attr('data-showtitle');
  var showportrait = $module.attr('data-showportrait');

  setTimeout(function() {
    var videoWrapper = $module.children('.vimeo_video');
    videoWrapper.empty().append('<div id="vimeo-wrapper-1643407133519"></div>');

    var options = {
      url: videoUrl,
      loop: videoloop,
      autoplay: autoPlay,
      autopause: autoPause,
      byline: byline,
      title: showtitle,
      muted: videoMute,
      portrait: showportrait
    };
    var player = new Vimeo.Player("vimeo-wrapper-1643407133519", options);

    player.on('loaded', function() {	
      var $playerFrame = $module.find('iframe');	
      var width = parseInt($playerFrame.attr('width'));	
      var height = parseInt($playerFrame.attr('height'));	
      if (width != NaN && height != NaN) {	
        $module.find('.videoFullScreen').css('padding-bottom', (height / width * 100) + '%');	
      }	
    });	
    
    $module.on('pausevideo', function() {	
      player.pause();	
    });
  }, 100);
})( window.GemQuery || jQuery );
    
    
(function( jQuery ){
  var $module = jQuery('#m-1643407167918').children('.module');
  var videoUrl = jQuery.trim($module.attr('data-url'));
  var autoPause = $module.attr('data-autopause');
  var autoPlay = $module.attr('data-autoplay');
  var videoloop = $module.attr('data-videoloop');
  var videoMute = $module.attr('data-videomute');
  var byline = $module.attr('data-byline');
  var showtitle = $module.attr('data-showtitle');
  var showportrait = $module.attr('data-showportrait');

  setTimeout(function() {
    var videoWrapper = $module.children('.vimeo_video');
    videoWrapper.empty().append('<div id="vimeo-wrapper-1643407167918"></div>');

    var options = {
      url: videoUrl,
      loop: videoloop,
      autoplay: autoPlay,
      autopause: autoPause,
      byline: byline,
      title: showtitle,
      muted: videoMute,
      portrait: showportrait
    };
    var player = new Vimeo.Player("vimeo-wrapper-1643407167918", options);

    player.on('loaded', function() {	
      var $playerFrame = $module.find('iframe');	
      var width = parseInt($playerFrame.attr('width'));	
      var height = parseInt($playerFrame.attr('height'));	
      if (width != NaN && height != NaN) {	
        $module.find('.videoFullScreen').css('padding-bottom', (height / width * 100) + '%');	
      }	
    });	
    
    $module.on('pausevideo', function() {	
      player.pause();	
    });
  }, 100);
})( window.GemQuery || jQuery );
    
    
(function( jQuery ){
  var $module = jQuery('#m-1643407204960').children('.module');
  var videoUrl = jQuery.trim($module.attr('data-url'));
  var autoPause = $module.attr('data-autopause');
  var autoPlay = $module.attr('data-autoplay');
  var videoloop = $module.attr('data-videoloop');
  var videoMute = $module.attr('data-videomute');
  var byline = $module.attr('data-byline');
  var showtitle = $module.attr('data-showtitle');
  var showportrait = $module.attr('data-showportrait');

  setTimeout(function() {
    var videoWrapper = $module.children('.vimeo_video');
    videoWrapper.empty().append('<div id="vimeo-wrapper-1643407204960"></div>');

    var options = {
      url: videoUrl,
      loop: videoloop,
      autoplay: autoPlay,
      autopause: autoPause,
      byline: byline,
      title: showtitle,
      muted: videoMute,
      portrait: showportrait
    };
    var player = new Vimeo.Player("vimeo-wrapper-1643407204960", options);

    player.on('loaded', function() {	
      var $playerFrame = $module.find('iframe');	
      var width = parseInt($playerFrame.attr('width'));	
      var height = parseInt($playerFrame.attr('height'));	
      if (width != NaN && height != NaN) {	
        $module.find('.videoFullScreen').css('padding-bottom', (height / width * 100) + '%');	
      }	
    });	
    
    $module.on('pausevideo', function() {	
      player.pause();	
    });
  }, 100);
})( window.GemQuery || jQuery );
    
    
(function( jQuery ){
  var $module = jQuery('#m-1643407257301').children('.module');
  var videoUrl = jQuery.trim($module.attr('data-url'));
  var autoPause = $module.attr('data-autopause');
  var autoPlay = $module.attr('data-autoplay');
  var videoloop = $module.attr('data-videoloop');
  var videoMute = $module.attr('data-videomute');
  var byline = $module.attr('data-byline');
  var showtitle = $module.attr('data-showtitle');
  var showportrait = $module.attr('data-showportrait');

  setTimeout(function() {
    var videoWrapper = $module.children('.vimeo_video');
    videoWrapper.empty().append('<div id="vimeo-wrapper-1643407257301"></div>');

    var options = {
      url: videoUrl,
      loop: videoloop,
      autoplay: autoPlay,
      autopause: autoPause,
      byline: byline,
      title: showtitle,
      muted: videoMute,
      portrait: showportrait
    };
    var player = new Vimeo.Player("vimeo-wrapper-1643407257301", options);

    player.on('loaded', function() {	
      var $playerFrame = $module.find('iframe');	
      var width = parseInt($playerFrame.attr('width'));	
      var height = parseInt($playerFrame.attr('height'));	
      if (width != NaN && height != NaN) {	
        $module.find('.videoFullScreen').css('padding-bottom', (height / width * 100) + '%');	
      }	
    });	
    
    $module.on('pausevideo', function() {	
      player.pause();	
    });
  }, 100);
})( window.GemQuery || jQuery );
    
    